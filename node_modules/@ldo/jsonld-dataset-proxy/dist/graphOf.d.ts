import type { GraphNode } from "@ldo/rdf-utils";
import type { ObjectLike } from "./types";
export declare function graphOf<Subject extends ObjectLike, Key extends keyof Subject>(subject: Subject, predicate: Key, object?: NonNullable<Subject[Key]> extends Array<unknown> ? number | ObjectLike : ObjectLike): GraphNode[];
