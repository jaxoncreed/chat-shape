import { MultiMap } from "../transformer/transformerSubTraversers/util/MultiMap";
import type { TraverserTypes } from "../traverser/TraverserTypes";
import { type InstanceNodeFor } from "./nodes/createInstanceNodeFor";
import type { TraverserDefinitions } from "../traverser/TraverserDefinition";
export declare class InstanceGraph<Types extends TraverserTypes<any>> {
    protected objectMap: MultiMap<object, keyof Types, InstanceNodeFor<Types, keyof Types>>;
    readonly traverserDefinitions: TraverserDefinitions<Types>;
    constructor(traverserDefinitions: TraverserDefinitions<Types>);
    getNodeFor<TypeName extends keyof Types>(instance: unknown, typeName: TypeName): InstanceNodeFor<Types, TypeName>;
}
