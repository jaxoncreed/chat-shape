import type { TransformerInputReturnTypes, TraverserDefinitions, TraverserTypes, VisitorsInput } from "..";
import { Transformer, Visitor } from "..";
import type { TransformersInput } from "../transformer/Transformers";
export declare class Traverser<Types extends TraverserTypes<any>> {
    private traverserDefinition;
    constructor(traverserDefinition: TraverserDefinitions<Types>);
    createTransformer<ReturnTypes extends TransformerInputReturnTypes<Types>, Context = undefined>(transformers: TransformersInput<Types, ReturnTypes, Context>): Transformer<Types, ReturnTypes, Context>;
    createVisitor<Context = undefined>(visitors: VisitorsInput<Types, Context>): Visitor<Types, Context>;
}
