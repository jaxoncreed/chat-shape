import type { TraverserDefinitions, TraverserTypes, VisitorsInput } from "../";
export declare class Visitor<Types extends TraverserTypes<any>, Context = undefined> {
    private traverserDefinition;
    private visitors;
    constructor(traverserDefinition: TraverserDefinitions<Types>, visitors: VisitorsInput<Types, Context>);
    private applyDefaultInterfaceVisitorProperties;
    private applyDefaultInterfaceVisitor;
    private applyDefaultUnionVisitor;
    private applyDefaultPrimitiveVisitor;
    private applyDefaultVisitors;
    visit<TypeName extends keyof Types>(item: Types[TypeName]["type"], itemTypeName: TypeName, context: Context): Promise<void>;
}
